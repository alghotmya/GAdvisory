# Optional: HTTPS URL that receives a POST with JSON on each page view / in-app navigation.
# Body shape: { event: "pageview", path, referrer, lang, ts }
# Your endpoint must send Access-Control-Allow-Origin for your deployed site (CORS).
# Do not commit real URLs with secrets; use a dedicated webhook (Zapier, Make, n8n, etc.).
# VITE_PAGE_VIEW_WEBHOOK_URL=https://hooks.example.com/your-id
